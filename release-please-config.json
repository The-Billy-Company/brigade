{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",

  "_comment": [
    "Conventional Commits → a version bump, a tag, and a GitHub Release.",
    "",
    "── One package, one version",
    "brigade ships to no package registry — it is a pure Zig package, fetched",
    "by consumers straight off a git tag. `build.zig.zon`'s `.version` is the",
    "only place the number lives, marked below with `x-release-please-version`.",
    "",
    "── Draft until the tarball is proven",
    "Unlike the crate/wheel repos in this org, there is no separate registry",
    "upload to gate — the tag itself IS the published artifact the instant it",
    "exists, because `zig fetch` resolves straight off",
    "`.../archive/refs/tags/vX.Y.Z.tar.gz`, which GitHub serves for any tag with",
    "no release object required. So this config leaves `draft: true`: merging",
    "the release PR tags the commit (making the tarball live — unavoidable, the",
    "same as every sibling repo's tag), but the human-facing Release page stays",
    "a draft until release.yml's `archive` job re-fetches that exact tarball,",
    "checks every path `build.zig.zon` declares is inside it, and proves a",
    "fresh `zig fetch --save` against it round-trips on all three OSes. Only",
    "then does the `publish-release` job flip the draft live.",
    "",
    "── Why `simple` + generic extra-files",
    "`simple` keeps the manifest as the source of truth; the `generic` updater",
    "rewrites the semver on any line carrying an `x-release-please-version`",
    "annotation — one line, in this repo. The shared",
    "`package-release version` command fails if that marked line is missing",
    "from this list, so the two cannot drift apart.",
    "",
    "── Changelog ownership",
    "towncrier owns CHANGELOG.md — the curated fragments under changelog.d/ are",
    "the release notes, written for people. release-please would compile a",
    "second, machine-derived changelog into the same file and fight it, so",
    "`skip-changelog` is on and the bot handles versions and tags only. The",
    "`fold changelog` job in release-please.yml builds the notes and commits",
    "them onto this PR's branch automatically; release.yml's preflight",
    "re-checks that the fold landed before it will touch the draft release.",
    "",
    "── What picks the number, while this is still 0.x",
    "The stock `default` strategy, plus the two `pre-major` settings below,",
    "which release-please reads ONLY while the version is under 1.0.0 — and",
    "this package is 0.1.0, so unlike the post-1.0 repos in this org they are",
    "live here and must stay. `bump-minor-pre-major` keeps a breaking change",
    "from declaring 1.0.0 on our behalf, and with",
    "`bump-patch-for-minor-pre-major` off a feature takes the minor too: a",
    "break and a feature both move 0.1.0 to 0.2.0, everything else to 0.1.1.",
    "Delete both keys in whatever release ships 1.0.0 — from then on they are",
    "read by nothing while still reading like a policy. The post-1.0 table,",
    "and the `Release-As: X.Y.Z` override that pins an exact version:",
    "https://github.com/The-Billy-Company/.github/blob/main/RELEASING.md#what-picks-the-number",
    "",
    "── The tag cascades",
    "Merging the release PR tags `vX.Y.Z`, which is what `release.yml` waits",
    "for. A tag pushed by GITHUB_TOKEN does NOT trigger another workflow, so",
    "that hand-off only happens because release-please runs as the",
    "`billy-company-release` GitHub App rather than as Actions — see",
    ".github/workflows/release-please.yml."
  ],

  "release-type": "simple",
  "include-component-in-tag": false,
  "include-v-in-tag": true,
  "separate-pull-requests": false,
  "draft": true,
  "prerelease": false,
  "bump-minor-pre-major": true,
  "bump-patch-for-minor-pre-major": false,

  "changelog-sections": [
    { "type": "feat", "section": "Features" },
    { "type": "fix", "section": "Bug Fixes" },
    { "type": "perf", "section": "Performance" },
    { "type": "refactor", "section": "Refactor" },
    { "type": "deps", "section": "Dependencies" },
    { "type": "revert", "section": "Reverts" },
    { "type": "docs", "section": "Documentation", "hidden": true },
    { "type": "test", "section": "Tests", "hidden": true },
    { "type": "build", "section": "Build", "hidden": true },
    { "type": "ci", "section": "CI", "hidden": true },
    { "type": "chore", "section": "Chore", "hidden": true },
    { "type": "style", "section": "Style", "hidden": true }
  ],

  "packages": {
    ".": {
      "release-type": "simple",
      "package-name": "brigade",
      "skip-changelog": true,
      "draft": true,
      "extra-files": [{ "type": "generic", "path": "build.zig.zon" }]
    }
  }
}
